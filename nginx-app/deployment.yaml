apiVersion: apps/v1
kind: Deployment
metadata:
  name: natasha-nginx   
spec:
  replicas: 3
  selector:
    matchLabels:
      app: natasha-app    
  template:
    metadata:
      labels:
        app: natasha-app  
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
